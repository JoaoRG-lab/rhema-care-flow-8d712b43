{
  // VS Code Copilot — .vscode/mcp.json
  // Documentação: https://code.visualstudio.com/docs/copilot/customization/mcp-servers
  //
  // Como usar:
  //   1. Cole o Supabase access token no prompt local do VS Code
  //   2. Defina o GitHub token no prompt local do VS Code
  //   3. Reinicie o VS Code
  //   4. Abra Copilot Chat → ícone de ferramentas → ative os servidores

  "inputs": [
    {
      "type": "promptString",
      "id": "github-pat",
      "description": "GitHub Personal Access Token (repo + read:org)",
      "password": true
    },
    {
      "type": "promptString",
      "id": "supabase-access-token",
      "description": "Supabase Access Token (read-only MCP)",
      "password": true
    }
  ],

  "servers": {

    "supabase": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase@latest", "--read-only"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${input:supabase-access-token}"
      }
    },

    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github@latest"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${input:github-pat}"
      }
    },

    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem@latest",
        "${workspaceFolder}/src",
        "${workspaceFolder}/supabase",
        "${workspaceFolder}/public"
      ]
    }

  }
}
